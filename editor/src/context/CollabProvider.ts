// import { EventDispatcher } from './utils/EventDispatcher'

import { ECollabActionType, CollabAction } from '@quick-prose/shared'

interface CollabProps {

}

export class CollabProvider {

  constructor(props?: CollabProps) {
  }

  
}
